use_frameworks!

target 'CaptureCenter_Example' do
  pod 'CaptureCenter', :path => '../'

  target 'CaptureCenter_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
  end
end
